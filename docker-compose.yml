services:
  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASS}
      POSTGRES_DB: ${DB_NAME}
    healthcheck: # ตรวจสอบว่า DB พร้อมรับแขกหรือยัง
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 5s
      timeout: 5s
      retries: 5
    ports:
      - "${DB_EXTERNAL_PORT}:5432"

  backend:
    build: ./backend
    ports:
      - "8180:8080"  
    depends_on:
      db:
        condition: service_healthy # รอจนกว่า db จะ Healthy จริงๆ
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_USER: ${DB_USER}
      DB_PASS: ${DB_PASS}
      DB_NAME: ${DB_NAME}

  frontend:
    build: ./frontend
    ports:
      - "${FRONTEND_PORT}:3000"
    environment:
      # ใช้ชื่อ service "backend" เพื่อคุยกันภายในวง Network ของ Docker
      BACKEND_URL: http://backend:8080/api/v1
    depends_on:
      - backend
  
  jenkins: 
    image: jenkins/jenkins:lts
    container_name: my-jenkins
    ports:
      - "8080:8080" # Jenkins จะยึดพอร์ต 8080
      - "50000:50000"
    volumes:
      - jenkins_home:/var/jenkins_home # แก้ไข path เล็กน้อยให้ตรงตามมาตรฐาน Jenkins Image
      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped

  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
      - ./prometheus/alert_rules.yml:/etc/prometheus/alert_rules.yml
      - ./prometheus-data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/usr/share/prometheus/console_libraries'
      - '--web.console.templates=/usr/share/prometheus/consoles'
    restart: unless-stopped

  grafana:
    image: grafana/grafana
    container_name: grafana
    ports:
      - "4000:3000"
    volumes:
      - grafana_storage:/var/lib/grafana
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    depends_on:
      - prometheus
volumes:
  grafana_storage:
  jenkins_home:
 