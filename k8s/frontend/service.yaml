apiVersion: v1
kind: Service
metadata:
  name: todo-frontend-service
  namespace: todo-app
spec:
  selector:
    app: todo-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP # ใช้ ClusterIP แล้วทำ Port-forward จะเสถียรที่สุดบนเครื่องส่วนตัว