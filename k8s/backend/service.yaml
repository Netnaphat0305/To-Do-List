apiVersion: v1
kind: Service
metadata:
  name: todo-backend-service
  namespace: todo-app
spec:
  selector:
    app: todo-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP # ให้ Frontend เรียกใช้ผ่านชื่อ Service ภายใน